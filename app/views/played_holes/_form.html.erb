<%= form_for(@played_hole) do |f| %>
  <% if @played_hole.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@played_hole.errors.count, "error") %> prohibited this played_hole from being saved:</h2>

      <ul>
      <% @played_hole.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
